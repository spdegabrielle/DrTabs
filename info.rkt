#lang info
(define name "Tabs+")
(define version "0.0.1")
(define release-notes '("for 5.x"))
(define blurb '("Tool that extends the DrRacket tabs"))

(define drracket-name "Tabs+")
(define drracket-tools (list (list "tool.rkt")))
